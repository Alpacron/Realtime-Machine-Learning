import React, { FC } from 'react';

interface AccountSignInProps { }

const AccountSignIn: FC<AccountSignInProps> = () => (
  <div data-testid="AccountSignIn">
    AccountSignIn Component
  </div>
);

export default AccountSignIn;
